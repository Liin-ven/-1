#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    if (b != 0 && a % b == 0)
        cout << "Да, b является делителем a\n";
    else
        cout << "Нет, b не является делителем a\n";

    return 0;
}
