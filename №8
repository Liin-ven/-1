#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    if (a != 0 && b % a == 0)
        cout << "Да, a является делителем b\n";
    else
        cout << "Нет, a не является делителем b\n";

    return 0;
}
